services:
  music-api:
    build: .
    container_name: music-api
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
