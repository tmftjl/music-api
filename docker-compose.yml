services:
  music-api:
    build: .
    container_name: music-api
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      TOKEN_SECRET: ${TOKEN_SECRET:-music-api-local-token-secret}
    restart: unless-stopped
